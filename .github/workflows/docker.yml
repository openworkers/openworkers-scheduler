on:
  push:
    branches:
      - main
      - dev
    tags:
      - "v*.*.*"
  pull_request:
    branches:
      - main
      - dev

jobs:
  build:
    name: Build
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        telemetry: [false, true]
    steps:
      - name: Check Out Repo
        uses: actions/checkout@v4
      - name: Docker metadata
        id: metadata
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' && !matrix.telemetry }}
            type=raw,value=latest-telemetry,enable=${{ github.ref == 'refs/heads/main' && matrix.telemetry }}
            type=ref,event=tag,suffix=${{ matrix.telemetry && '-telemetry' || '' }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v5
        with:
          file: Dockerfile.multi
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.metadata.outputs.tags }}
          build-args: |
            TELEMETRY=${{ matrix.telemetry }}
          cache-from: type=gha,scope=main-${{ matrix.telemetry }}
          cache-to: type=gha,mode=max,scope=main-${{ matrix.telemetry }}
          platforms: linux/amd64,linux/arm64
